

#include "TASK8.h"
float smallestnum (float x, float y , float z ){

    float smallest = x;

        if (y < smallest)
        {
        smallest = y;
        }

            if (z < smallest)
            {
            smallest = z;
            }

        return smallest;

                                               }
